{
  "name": "react-scroll-navigation",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "build": "lerna run build",
    "watch:hook": "yarn workspace use-scroll-navigation watch",
    "watch:materialui-example": "yarn workspace materialui-example start"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
